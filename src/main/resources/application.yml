spring:
  application:
    name: ms-account
  config:
    import: "optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}"
  cloud:
    config:
      username: admin
      password: local123